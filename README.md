# 数据运维工作台

本目录是数据运维工作台项目的共享正式目录。

协作规则：
- 默认用户只和 `斯维因` 沟通。
- `维克托` 和 `巴德` 默认只听 `斯维因` 调度。
- 正式交付物、状态、任务、评审都写在本目录。
- 群 `oc_5e1652a779630341d5d6d10da805da59` 只做通知和可见性补充，不替代正式交接。

当前角色：
- 斯维因：产品经理 / 默认单入口
- 维克托：全栈工程师
- 巴德：测试 / 质量
