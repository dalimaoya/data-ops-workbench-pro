const systemInfoContent = `# 数据运维工作台 - 系统说明

> 版本：v1.0  
> 更新日期：2026-03-24  
> 适用系统版本：v2.6.0+

---

## 一、产品定位

数据运维工作台是一个**轻量级的数据安全修订平台**，专为政企项目交付和运维现场设计。

它的定位是：围绕数据库中的结果数据表，提供「读取 → 模板导出 → Excel 修订 → 校验预览 → 安全回写 → 留痕回退」的轻量工作台。

**它是什么：**
- 结果数据表的安全修订工具
- 让非技术人员能自主完成数据维护的标准化平台
- 可复用、可开源的运维提效成果物

**它不是什么：**
- 不是 SQL 开发平台或数据库管理工具（如 Navicat、DBeaver）
- 不是 BI 分析和数据可视化平台
- 不是传统的数据填报系统
- 不是审批 OA 系统（虽然有简单审批流）
- 不是主数据治理平台

---

## 二、产品目标

### 核心目标
让普通运维人员或业务维护人员，在**不直接接触数据库**的前提下，完成结果数据的安全更新。

### 次级目标
把研发的一次性配置，转换成后续普通用户可重复执行的标准流程。

### 沉淀目标
形成一个可复用、可演示、可开源的工作台成果物，用于：
- 项目交付提效
- 运维流程规范化
- 个人方法论沉淀
- 案例展示与品牌输出

---

## 三、核心设计原则

### 3.1 安全第一

数据修改是高风险操作，系统在每个环节都加了安全保障：

\`\`\`
上传模板 → ① 模板合法性校验（是不是平台导出的？）
         → ② 数据类型/必填/枚举/长度校验
         → ③ 主键不可变校验
         → ④ 差异预览（让用户看清改了什么）
         → ⑤ 确认回写（用户主动确认）
         → ⑥ 写前全表备份
         → ⑦ 执行写入
         → ⑧ 结果反馈 + 日志记录
         → ⑨ 出问题可版本回退
\`\`\`

任何一个环节出错，都会阻断后续操作，确保不会"糊里糊涂改了数据"。

### 3.2 平台模板机制

**为什么不让用户随便上传 Excel？**

因为：
- 列名可能和数据库字段不对应
- 数据格式可能不对
- 可能多列少列
- 无法追溯这个 Excel 是基于哪个版本的数据导出的

所以系统要求所有修订必须通过「平台导出的标准模板」完成：
- 模板中嵌入隐藏的元信息工作表（_meta），记录版本、表标识、配置版本等
- 上传时系统验证元信息，非平台模板直接拒绝
- 模板使用 Excel 工作表保护，表头和主键列锁定

### 3.3 最小权限原则

| 设计 | 效果 |
|---|---|
| 三角色分权 | 管理员配置、操作员执行、只读查看 |
| 数据源级权限 | 不同用户只看到授权的数据源 |
| 字段级权限 | 指定字段只有管理员能编辑 |
| 连接信息隔离 | 普通用户看不到数据库连接信息 |
| 可选审批流 | 操作员的回写可以要求管理员审核 |

### 3.4 一次配置、反复使用

研发或管理员完成数据源和表的配置后，后续修订流程完全由普通用户独立完成。不需要每次都找研发写 SQL。

---

## 四、系统架构

### 4.1 总体架构

\`\`\`
┌─────────────────────────────────────────────┐
│              浏览器（用户端）                   │
│   React 19 + Ant Design 6 + react-i18next    │
└──────────────────┬──────────────────────────┘
                   │ HTTP API
┌──────────────────▼──────────────────────────┐
│           FastAPI 后端服务                     │
│                                              │
│  ┌──────────┐  ┌──────────┐  ┌────────────┐ │
│  │ 业务路由   │  │ 认证中间件 │  │ i18n 中间件│ │
│  │(数据源/表/ │  │(JWT+验证码)│  │(多语言)   │ │
│  │ 数据维护)  │  └──────────┘  └────────────┘ │
│  └────┬─────┘                                │
│       │                                      │
│  ┌────▼─────┐  ┌──────────────────────────┐  │
│  │ 平台数据库 │  │ 多数据库连接器              │  │
│  │ (SQLite)  │  │ MySQL/PG/SS/Oracle/DM/   │  │
│  │ 配置/日志  │  │ KingbaseES/SQLite        │  │
│  └──────────┘  └──────────────────────────┘  │
└──────────────────────────────────────────────┘
\`\`\`

### 4.2 技术栈

| 层级 | 技术 | 选型理由 |
|---|---|---|
| **前端框架** | React 19 + TypeScript | 生态成熟，组件丰富 |
| **UI 库** | Ant Design 6 | 企业级 UI，表格/表单能力强 |
| **前端构建** | Vite | 开发快，打包小 |
| **国际化** | react-i18next + 后端 i18n | 前后端统一多语言 |
| **后端框架** | Python FastAPI | 异步高性能，自动 API 文档 |
| **ORM** | SQLAlchemy 2.0 | 多数据库支持 |
| **平台数据库** | SQLite | 零配置，文件级，拷贝即走 |
| **认证** | JWT + bcrypt | 24 小时有效期，密码哈希存储 |
| **Excel** | openpyxl | Python Excel 读写 |

### 4.3 数据流

\`\`\`
用户操作                    系统处理                     数据存储
───────                    ──────                     ──────
导出模板 ──────────────→ 从业务库读数据 ──────────→ 生成 Excel 下载
上传模板 ──────────────→ 解析 Excel ──────────────→ 临时存储
校验 ───────────────────→ 比对配置+检查类型 ────→ 校验结果
差异预览 ──────────────→ 比对原数据和新数据 ───→ 展示差异
确认回写 ──────────────→ 备份 → 执行 SQL ─────→ 业务库 + 日志
版本回退 ──────────────→ 读备份 → 恢复 ────────→ 业务库 + 日志
\`\`\`

---

## 五、功能模块详细说明

### 5.1 数据源管理

管理系统连接的外部数据库。

| 功能 | 说明 |
|---|---|
| 新增数据源 | 填写连接信息，支持 7 种数据库 |
| 编辑数据源 | 修改连接参数 |
| 删除数据源 | 删除后该数据源下的纳管表配置也会清除 |
| 测试连接 | 验证连接参数是否正确 |
| 数据源列表 | 支持按数据库类型筛选 |

**支持的数据库：** MySQL / PostgreSQL / SQL Server / Oracle / 达梦(DM) / 人大金仓(KingbaseES) / SQLite

### 5.2 纳管表配置

将数据库中的表纳入平台管理。

| 功能 | 说明 |
|---|---|
| 选择纳管表 | 从数据源中选择要管理的表 |
| 自动拉取字段 | 系统自动读取表的所有字段及类型 |
| 字段配置 | 设置每个字段的展示、编辑、导出等属性 |
| 主键设置 | 指定单主键或复合主键 |
| 表结构检测 | 自动检测表结构是否发生变化 |
| 字段同步 | 表结构变化后同步最新字段 |

### 5.3 数据维护

日常数据查看和修改的主操作区。

| 功能 | 说明 |
|---|---|
| 数据浏览 | 分页展示，支持搜索和筛选 |
| 在线编辑 | 页面内直接修改数据（少量修改） |
| 新增行 | 单条或批量新增数据 |
| 删除行 | 勾选删除，自动备份 |
| 模板导出 | 全量或筛选导出，含元信息保护 |
| 批量导出 | 多表打包为 ZIP |
| 模板导入 | 上传模板，自动校验 |
| 差异预览 | 原值/新值逐行对比 |
| 对比报告导出 | 差异明细导出为 Excel |
| 安全回写 | 备份 + 写入 + 日志 |

### 5.4 审批流

| 功能 | 说明 |
|---|---|
| 全局开关 | 管理员控制是否启用 |
| 提交审批 | 操作员回写时自动进入审批 |
| 审批处理 | 管理员查看详情后通过或拒绝 |
| 审批通知 | 状态变更时通知相关人 |

### 5.5 用户与权限

| 功能 | 说明 |
|---|---|
| 用户管理 | 增删改查、角色分配、禁用/启用 |
| 三角色体系 | 管理员 / 操作员 / 只读 |
| 数据源权限 | 按用户分配可访问的数据源 |
| 字段级权限 | 指定字段只有管理员能编辑 |
| 个人设置 | 修改密码、修改显示名 |

### 5.6 日志与审计

| 功能 | 说明 |
|---|---|
| 系统操作日志 | 所有操作的流水记录 |
| 导出日志 | 导出操作的详细记录 |
| 导入日志 | 导入操作和校验结果记录 |
| 回写日志 | 回写操作 + 逐字段变更明细 |
| 异步任务日志 | 大表异步导出的进度 |

### 5.7 版本回退

| 功能 | 说明 |
|---|---|
| 备份列表 | 按表/时间查看历史备份 |
| 版本详情 | 查看备份的数据量和操作人 |
| 一键回退 | 恢复到指定版本（回退前也会备份当前数据） |

### 5.8 通知中心

| 功能 | 说明 |
|---|---|
| 站内通知 | 回写完成、审批状态、异步导出完成 |
| 未读标记 | 铃铛图标显示未读数 |
| 跳转关联 | 点击通知跳转到相关页面 |

### 5.9 国际化

| 功能 | 说明 |
|---|---|
| 中英文切换 | 一键切换，即时生效 |
| 偏好保存 | 记住用户的语言选择 |
| 全覆盖 | 菜单、按钮、提示、表头等全部支持 |
| 前后端统一 | 前端 i18n + 后端 i18n 中间件 |

---

## 六、部署说明

### 6.1 部署模式

| 模式 | 适用场景 | 说明 |
|---|---|---|
| **源码运行** | 开发/测试环境 | 需要 Python 3.9+ 和 Node.js 18+ |
| **独立打包** | 生产/内网环境 | PyInstaller 打包，零依赖运行 |
| **Docker** | 容器化部署 | docker-compose 一键启动 |

### 6.2 系统要求

| 项目 | 最低要求 | 推荐配置 |
|---|---|---|
| CPU | 1 核 | 2 核+ |
| 内存 | 512 MB | 1 GB+ |
| 磁盘 | 500 MB | 2 GB+（取决于备份数据量） |
| 操作系统 | Linux / Windows / macOS | CentOS 7+ / Ubuntu 20.04+ / Windows 10+ |
| 浏览器 | Chrome 80+ / Firefox 80+ / Edge 80+ | 最新版本 |

### 6.3 端口说明

| 端口 | 用途 | 可否修改 |
|---|---|---|
| 8580 | Web 服务 | 可通过 PORT 环境变量修改 |

### 6.4 数据目录

| 目录 | 内容 | 备份建议 |
|---|---|---|
| \`data/\` | 平台 SQLite 数据库、上传文件 | ⚠️ 必须备份 |
| \`backups/\` | 表数据备份文件 | ⚠️ 建议备份 |
| \`logs/\` | 运行日志 | 可选 |

### 6.5 迁移方式

当前版本迁移方式：将整个工作台文件夹（含 \`data/\` 和 \`backups/\`）拷贝到新机器。

v3.0 将提供一键备份与迁移功能：
- 一键导出平台数据为 ZIP 包
- 在新机器一键导入恢复
- 敏感信息（密码、Key）脱敏处理

---

## 七、安全架构

### 7.1 认证安全

| 措施 | 说明 |
|---|---|
| 图形验证码 | 登录时需输入，5 分钟有效，防暴力破解 |
| JWT Token | 24 小时有效期，支持自定义签名密钥 |
| 密码存储 | bcrypt 哈希，不存明文 |
| 会话管理 | Token 过期自动跳转登录页 |

### 7.2 权限安全

| 措施 | 说明 |
|---|---|
| 前端菜单隔离 | 不同角色看到不同菜单 |
| 后端 API 拦截 | 每个接口都有角色校验，越权请求直接拒绝 |
| 数据源隔离 | 非授权数据源的数据完全不可见 |
| 审批流 | 操作员的敏感操作可配置审批 |

### 7.3 数据安全

| 措施 | 说明 |
|---|---|
| 写前备份 | 每次回写/删除/回退前自动全表备份 |
| 版本回退 | 可恢复到任意备份版本 |
| 模板防篡改 | 非平台模板无法导入 |
| 多维度校验 | 类型/必填/枚举/长度/主键 全面检查 |
| 操作审计 | 所有操作记录日志，含操作人和详细内容 |
| 逐字段日志 | 回写日志记录每个字段的 old → new |

---

## 八、API 说明

系统提供完整的 RESTful API，启动后访问 \`http://localhost:8580/docs\` 查看自动生成的 Swagger 文档。

### 主要 API 分组

| 分组 | 路径前缀 | 说明 |
|---|---|---|
| 认证 | \`/api/auth/\` | 登录、验证码、Token 刷新 |
| 数据源 | \`/api/datasources/\` | 数据源 CRUD、测试连接 |
| 纳管表 | \`/api/table-configs/\` | 表配置 CRUD、字段管理 |
| 数据维护 | \`/api/data/\` | 数据浏览、导出、导入、校验、回写 |
| 日志 | \`/api/logs/\` | 日志查询 |
| 版本回退 | \`/api/backups/\` | 备份列表、回退操作 |
| 用户 | \`/api/users/\` | 用户管理 |
| 审批 | \`/api/approvals/\` | 审批操作 |
| 通知 | \`/api/notifications/\` | 通知查询、标记已读 |
| 系统 | \`/api/system/\` | 系统设置 |

---

## 九、版本历史

| 版本 | 日期 | 主要内容 |
|---|---|---|
| v1.0 | 2026-03-20 | MVP 交付：数据源管理、纳管表配置、模板导出导入、校验回写、日志、版本回退 |
| v1.1 | 2026-03-21 | 时间格式统一、状态中文化、关于页面 |
| v1.2 | 2026-03-21 | 模板 Excel 保护、导入校验增强 |
| v1.3 | 2026-03-22 | 用户管理、角色权限、数据源权限 |
| v1.4 | 2026-03-22 | PyInstaller 独立打包 |
| v2.0-v2.6 | 2026-03-23 | 新增/删除行、在线编辑、审批流、通知中心、异步导出、批量导出、差异报告导出、验证码、多语言、Oracle/达梦/金仓/SQLite 适配 |
| v3.0 | 规划中 | AI 智能化（7 个场景）、大模型平台接入、多表变更对比、平台数据备份与迁移 |

---

## 十、未来规划（v3.0）

### AI 智能化

| 功能 | 说明 |
|---|---|
| 智能字段配置 | AI 根据字段名和样本数据推荐配置 |
| 智能数据校验 | 基于历史数据检测异常值和格式问题 |
| 自然语言查询 | 用大白话描述查询条件 |
| 日志智能分析 | 操作摘要、异常检测、问题溯源 |
| 模板智能填充 | 自然语言描述批量修改规则 |
| 数据源健康巡检 | 定时巡检连接状态和结构变化 |
| 回写影响评估 | 评估修改的潜在影响和风险 |

### 大模型接入

支持预设 9 个常见大模型平台（DeepSeek、通义千问、硅基流动、智谱、百度文心、Kimi、OpenAI、Claude、零一万物），也支持自定义 API 接口。兼容 OpenAI 和 Claude 两种 API 协议。

### 平台数据备份与迁移

一键导出/导入整个平台的配置和数据，支持跨机器迁移。

---

## 变更记录

| 日期 | 版本 | 变更 |
|---|---|---|
| 2026-03-24 | v1.0 | 首版系统说明文档 |
`;

export default systemInfoContent;
