# 维克托 — v5.0 统一认证 8443 入口修正回传

- 日期：2026-03-28
- 任务：统一认证平台联调入口修正为 8443
- 结论：**已完成工作台侧最小修正，并已完成重新构建校验；建议立即进入复测。**

## 一、修正范围

本次仅执行工作台侧最小修正，不扩展到认证平台本体，不改动其他业务范围。

修正内容如下：

1. **统一认证平台 base URL 默认口径修正为 8443**
   - 后端统一认证服务默认地址已从 `https://auth.aiusing.net` 调整为 `https://auth.aiusing.net:8443`
   - 仍保留环境变量 `AUTH_PLATFORM_BASE_URL` 覆盖能力，满足“可配置”要求

2. **统一认证相关入口随 base URL 同步切换到 8443**
   - 微信登录跳转：`/api/auth/wechat/redirect-url` 代理到认证平台时，已随统一 base URL 切换
   - 在线校验：`/api/auth/verify` 代理到认证平台时，已随统一 base URL 切换
   - 插件授权：`/api/license/check` 代理到认证平台时，已随统一 base URL 切换
   - 公钥下载：`/public/keys/jwt_public.pem` 拉取地址已随统一 base URL 切换

3. **登录页联调展示口径同步修正**
   - 前端登录页提示文案已明确当前联调入口为 `https://auth.aiusing.net:8443`
   - 避免继续按 443 口径误导测试与联调

## 二、变更文件

### 代码文件
1. `backend/app/services/unified_auth.py`
2. `frontend/src/pages/Login.tsx`

### 构建产物
1. `backend/web/index.html`
2. `backend/web/assets/index-CCQ6BRZC.js`
3. `backend/web/assets/index-CFltm5TR.css`

## 三、当前 base URL 口径

### 当前联调默认口径
> **https://auth.aiusing.net:8443**

### 当前实现方式
- 默认值：`https://auth.aiusing.net:8443`
- 可配置项：`AUTH_PLATFORM_BASE_URL`
- 即：如后续环境需要切换，仍可通过环境变量覆盖；当前未显式配置时，工作台将默认走 8443

## 四、具体修正说明

### 1. 后端统一认证接线
`backend/app/services/unified_auth.py` 中统一认证基础地址默认值已改为 8443，因此以下调用全部同步生效：

- `build_wechat_redirect_url()`
- `verify_token_online()`
- `check_plugin_license()`
- `download_public_key()`

这意味着登录跳转、在线 verify、license check、公钥下载四条链路已统一切换到正确入口，无需分别硬编码修补。

### 2. 前端登录入口展示
`frontend/src/pages/Login.tsx` 已将登录页展示文案改为：

- 登录入口：统一认证平台（当前联调：8443）
- 认证来源：`https://auth.aiusing.net:8443`

用于对齐当前联调口径，减少测试误判。

## 五、构建校验结果

### 1. 前端重新构建
已执行：
```bash
cd /home/node/.openclaw/workspace/projects/data-ops-workbench/frontend
npm run build
```

结果：**通过**

构建输出：
- `backend/web/index.html`
- `backend/web/assets/index-CCQ6BRZC.js`
- `backend/web/assets/index-CFltm5TR.css`

说明：Vite 有既有的大包体 warning，但**不阻断本次构建通过**。

### 2. 后端语法校验
已执行：
```bash
python3 -m compileall /home/node/.openclaw/workspace/projects/data-ops-workbench/backend/app
```

结果：**通过**

说明：`backend/app/services/unified_auth.py` 编译通过，未见语法错误。

## 六、是否建议立即复测

**建议：是，建议立即复测。**

建议复测重点：
1. 从工作台登录页发起微信登录，确认是否跳转到 `https://auth.aiusing.net:8443` 对应统一认证链路
2. 回跳后验证 `/api/auth/verify` 是否可正常建立在线登录态
3. 验证至少 1 个插件授权校验链路（建议 `plugin-ai-assistant` 或 `plugin-data-trend`）
4. 验证公钥拉取与离线验签兜底是否正常

## 七、当前结论

本次 8443 入口修正已在工作台侧完成最小闭环处理：

- base URL 默认口径已修正为 8443
- 登录跳转 / verify / license check / 公钥下载 已统一跟随切换
- 已完成重新构建校验
- 当前可直接进入联调复测阶段
