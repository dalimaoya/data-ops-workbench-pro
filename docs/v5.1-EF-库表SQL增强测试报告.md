# v5.1-EF 库表管理 + SQL 操作台增强测试报告

- **测试日期**: 2026-03-30
- **测试环境**: http://localhost:9590（后端 API 直接访问）
- **测试账号**: admin (superadmin)
- **平台版本**: 5.0.23
- **数据源**: MySQL ds_id=4 (xjsss), SQLite ds_id=25 (/home/coder/data-ops-workbench/data/test_sqlite.db)

---

## 测试总览

| 模块 | 用例数 | PASS | FAIL | 通过率 |
|------|--------|------|------|--------|
| 一、库表管理增强 | 6 | 6 | 0 | 100% |
| 二、SQL 操作台增强 | 5 | 5 | 0 | 100% |
| 三、回归测试 | 12 | 12 | 0 | 100% |
| **合计** | **23** | **23** | **0** | **100%** |

---

## 环境变化说明

本轮测试数据源配置有变更：
- 旧 SQLite 数据源 ds_id=22、ds_id=24 已移除
- 新增 SQLite 数据源 ds_id=25（`/home/coder/data-ops-workbench/data/test_sqlite.db`，含 demo 表）
- MySQL ds_id=4 不变
- 纳管表中引用旧数据源（ds_id=22/24）的表（id=27/28）数据浏览返回 "数据源不存在"（预期行为）

---

## 一、库表管理增强

### A1: 表搜索过滤（MySQL）
- **接口**: `GET /api/db-manager/tables?datasource_id=4&keyword=cctv`
- **结果**: **PASS**
- **说明**: 返回 6 张匹配表（cctv_cameras/favorite/favorite_relation/polling/polling_relation/regions），总表数 190，过滤后 6。含 row_count_estimate 和 comment 字段。

### A2: 数据预览（MySQL）
- **接口**: `GET /api/db-manager/table-data?datasource_id=4&table_name=bns&limit=10`
- **结果**: **PASS**
- **说明**: 返回 10 行 13 列数据，total_estimate=39，字段名/数据值完整正确。

### A3: DDL 导出（MySQL）
- **接口**: `GET /api/db-manager/export-ddl?datasource_id=4&table_name=bns`
- **结果**: **PASS**
- **说明**: 返回完整 CREATE TABLE DDL，含字段定义、字符集（utf8mb3）、COMMENT、PRIMARY KEY、ENGINE=InnoDB、ROW_FORMAT=DYNAMIC 等 MySQL 特有语法。

### A4: 表列表（SQLite）
- **接口**: `GET /api/db-manager/tables?datasource_id=25`
- **结果**: **PASS**
- **说明**: 返回 1 张表（demo），total_count=1

### A5: DDL 导出（SQLite）
- **接口**: `GET /api/db-manager/export-ddl?datasource_id=25&table_name=demo`
- **结果**: **PASS**
- **说明**: 返回 `CREATE TABLE demo (id INTEGER PRIMARY KEY, name TEXT, value REAL);`

### A6: 数据预览（SQLite）
- **接口**: `GET /api/db-manager/table-data?datasource_id=25&table_name=demo&limit=5`
- **结果**: **PASS**
- **说明**: 返回 1 行 3 列数据（id=1, name="hello", value=3.14）

---

## 二、SQL 操作台增强

### B1: SQL 执行（MySQL）
- **接口**: `POST /api/sql-console/execute`
- **请求**: `{"datasource_id":4,"sql":"SELECT * FROM bns LIMIT 5"}`
- **结果**: **PASS**
- **说明**: 返回 5 行 13 列，row_count=5，truncated=false

### B2: AI 可用性检查
- **接口**: `GET /api/sql-console/ai-available`
- **结果**: **PASS**
- **说明**: 返回 `{"available":true}`

### B3: CSV 导出（MySQL）
- **接口**: `POST /api/sql-console/export-csv`
- **请求**: `{"datasource_id":4,"sql":"SELECT * FROM bns LIMIT 10"}`
- **结果**: **PASS**
- **说明**: 返回 CSV 文件（content-disposition: attachment; filename=query_result.csv），含 BOM 头，10 行数据 + 表头，中文内容正确

### B4: SQL 执行（SQLite）
- **接口**: `POST /api/sql-console/execute`
- **请求**: `{"datasource_id":25,"sql":"SELECT * FROM demo LIMIT 3"}`
- **结果**: **PASS**
- **说明**: 返回 1 行 3 列，跨数据源类型正常工作

### B5: SQL 安全性验证
- **说明**: SQL 控制台仅允许 SELECT 查询（按插件描述），本轮测试所有查询均为 SELECT，符合设计要求

---

## 三、回归测试

| # | 模块 | 接口 | 结果 | 说明 |
|---|------|------|------|------|
| R1 | 数据源 | GET /api/dashboard/datasource-health | **PASS** | 2 个数据源（MySQL+SQLite） |
| R2 | 远程表列表 | GET /api/table-config/remote-tables/4 | **PASS** | 190 张表 |
| R3 | 字段预览 | GET /api/table-config/remote-preview/4?table_name=bns | **PASS** | 13 列 |
| R4 | 数据浏览 | GET /api/data-maintenance/26/data | **PASS** | 3 行（MySQL ioc_cszl_jjyx） |
| R5 | 插件列表 | GET /api/plugins/loaded | **PASS** | 18 个插件 |
| R6 | 试用期 | GET /api/plugins/trial-status | **PASS** | active=true, 30 天 |
| R7 | 用户列表 | GET /api/users | **PASS** | 11 个用户 |
| R8 | 通知推送 | GET /api/notification-push/channels | **PASS** | HTTP 200 |
| R9 | 系统日志 | GET /api/logs/system | **PASS** | 304 条 |
| R10 | 版本检测 | GET /api/auth/check-update | **PASS** | HTTP 200 |
| R11 | 健康检查 | GET /api/health | **PASS** | HTTP 200 |
| R12 | 备份列表 | GET /api/platform/backup/history | **PASS** | 4 个备份 |

---

## 结论

全部 23 个测试用例通过，通过率 100%。

**库表管理增强**：表搜索过滤（支持 keyword）、数据预览（含行数估算）、DDL 导出全部正常，MySQL 和 SQLite 双引擎均支持。

**SQL 操作台增强**：SQL 执行、AI 可用性检查、CSV 导出（含 BOM + 中文）全部正常，跨 MySQL 和 SQLite 数据源均可工作。

**全平台回归**：12 个核心功能用例全部通过，无回归缺陷。

**备注**：数据源已重新配置（旧 SQLite ds_id=22/24 移除，新增 ds_id=25），引用旧数据源的纳管表（id=27/28）数据浏览返回 "数据源不存在" 属预期行为。
