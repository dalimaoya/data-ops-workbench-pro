# v3.2 P0 BUG 修复复测报告

> **测试人**：巴德（QA）  
> **复测日期**：2026-03-24  
> **修复内容**：BUG-001 — SPA catch-all 路由移到插件加载之后  
> **结论**：🟢 **P0 BUG 已修复，复测通过**

---

## 一、未认证访问测试（应返回 401）

| # | 接口 | 期望 | 实际 | 结果 |
|---|---|---|---|---|
| 1 | `GET /api/notifications` | 401 | 401 | ✅ |
| 2 | `GET /api/approvals` | 401 | 401 | ✅ |
| 3 | `GET /api/scheduler/tasks` | 401 | 401 | ✅ |
| 4 | `GET /api/health-check/config` | 401 | 401 | ✅ |
| 5 | `GET /api/ai/config` | 401 | 401 | ✅ |
| 6 | `GET /api/platform/backup/history` | 401 | 401 | ✅ |
| 7 | `GET /api/db-manager/tables` | 401 | 401 | ✅ |
| 8 | `GET /api/plugins/loaded` | 200 | 200 | ✅ |

**8/8 通过** — 之前全部返回 `{"detail":"Not Found"}` 的问题已消除。

---

## 二、认证后数据验证

使用 admin 账号登录获取 JWT token，验证关键 GET 接口返回正确数据：

| 接口 | 结果 | 返回数据 |
|---|---|---|
| `GET /api/notifications` | ✅ | 3 条通知 |
| `GET /api/approvals` | ✅ | 正常返回审批数据 |
| `GET /api/scheduler/tasks` | ✅ | 2 个定时任务 |
| `GET /api/health-check/config` | ✅ | 4 条巡检配置 |
| `GET /api/ai/config` | ✅ | AI 配置（ai_enabled, engine_mode 等） |
| `GET /api/platform/backup/history` | ✅ | 2 条备份记录 |
| `GET /api/db-manager/tables?datasource_id=4` | ✅ | 204 张表 |
| `GET /api/plugins/loaded` | ✅ | 10 个插件全部加载 |

**8/8 通过** — 认证后均可正常获取业务数据。

---

## 三、复测结论

| 项目 | 状态 |
|---|---|
| BUG-001（P0 阻断）修复验证 | ✅ 已修复 |
| 插件 GET 路由可达性 | ✅ 全部恢复 |
| 认证鉴权正常 | ✅ 未认证返回 401，认证后返回数据 |
| 插件加载完整性 | ✅ 10/10 插件正常 |

**BUG-001 修复确认通过，v3.2 P0 阻断问题已解除。**

---

## 四、遗留问题（非本次修复范围）

以下问题在上一轮测试中已记录，非 P0，不影响本次发布判定：

| 编号 | 级别 | 问题 | 状态 |
|---|---|---|---|
| BUG-002 | P2 | 定时任务启动加载失败 | 待修复 |
| BUG-003 | P2 | 健康巡检用 display_name 查表导致误报 | 待修复 |
| OPT-001 | P3 | 建表不支持 AUTO_INCREMENT | 建议优化 |

---

*复测完成：2026-03-24 17:20 UTC*
